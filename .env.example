# Flask Configuration
FLASK_DEBUG=False
HOST=0.0.0.0
PORT=5000

# Optional API Keys
OPENWEATHER_API_KEY=your_openweather_api_key_here

# SoilGrids API doesn't require API key but has usage limits
# For production, consider implementing caching and rate limiting
